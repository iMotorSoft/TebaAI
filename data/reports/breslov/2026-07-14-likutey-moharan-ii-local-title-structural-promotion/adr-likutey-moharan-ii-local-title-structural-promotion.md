# ADR — Promoción por bordes locales

Los rangos se promueven entre inicios con título hebreo local verificado; PDF 403 se promueve por etiqueta explícita de diagramas. Las páginas restantes se conservan citables y sin clasificación. Rollback: borrar sólo el detector run local-title.

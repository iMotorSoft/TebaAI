# AI zone candidate promotion validator

Read-only deterministic validator. It rejects AI claims that contradict blank/diagram/appendix context or use unverifiable quotes; ambiguous notes remain review candidates. It never writes corpus state.

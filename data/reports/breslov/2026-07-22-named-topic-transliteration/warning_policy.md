# Warnings

Los reason codes permanecen en el contrato técnico. La vista principal filtra `ai_interpretation_fallback:*`; el detalle contraído informa que se usó el analizador determinístico, sin exponer `ValidationError`. Warnings se deduplican.

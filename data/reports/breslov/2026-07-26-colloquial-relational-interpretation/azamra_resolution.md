# Azamra resolution

The corpus and existing relation evidence configuration already contain Azamra. It was not present in the concept catalog used by query understanding.

A minimal catalog record now provides:

- canonical label: `Azamra`;
- normalized case-equivalent alias: `azamra`;
- Hebrew orthography: `אזמרה`;
- subject type at interpretation time: `conceptual_term`.

No doctrinal definition or artificial related concept was added. Parser behavior remains generic; canonicalization applies only when the catalog label is case-equivalent to the extracted subject.

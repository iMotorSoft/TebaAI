# Display template policy

`concept_cooccurrence` renders from validated structured fields:

> Interpreté que desea investigar con qué conceptos se relaciona {subject}.

Binary `relation_query` retains:

> Interpreté que desea investigar la relación entre «{left}» y «{right}».

The generic quoted-query template is reserved for genuinely unknown input. AI prose is never rendered directly.
